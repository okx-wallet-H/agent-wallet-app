package com.agentwallet.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Background ──────────────────────────────────────────
val BackgroundPrimary   = Color(0xFF09090D)  // near-black with blue hint
val BackgroundSecondary = Color(0xFF131318)  // card / bubble surface
val BackgroundTertiary  = Color(0xFF1A1A22)  // dialog / sheet / input field
val BackgroundGlass     = Color(0x0AFFFFFF)  // 4% white over blur

// ─── Text ───────────────────────────────────────────────
val TextPrimary   = Color(0xFFEDEDF0)
val TextSecondary = Color(0xFF8B8B98)
val TextTertiary  = Color(0xFF5C5C6A)
val TextOnAccent  = Color(0xFFFFFFFF)

// ─── Accent (Agent persona — purple-blue) ────────────────
val Accent       = Color(0xFF6C5CE7)
val AccentGlow   = Color(0xFFA78BFA)
val AccentSubtle = Color(0x1F6C5CE7)  // 12% opacity

// ─── Semantic (restrained) ──────────────────────────────
val Profit  = Color(0xFF00D897)  // muted teal-green
val Loss    = Color(0xFFFF6B6B)  // warm red
val Warning = Color(0xFFFFA726)  // amber
val Info    = Color(0xFF5C9CFF)  // neutral blue

// ─── Borders & Dividers ─────────────────────────────────
val BorderSubtle  = Color(0x0FFFFFFF)  // 6% white
val DividerSubtle = Color(0x0AFFFFFF)  // 4% white
